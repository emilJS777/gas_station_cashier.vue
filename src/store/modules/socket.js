// import { io } from 'socket.io-client';


const socket = {
    namespaced: true,
    mutations: {

    },
    // state: {
    //     socket: io.connect("http://localhost:5001", {
    //         extraHeaders: {
    //             Authorization: localStorage.getItem("access") || null
    //         }
    //     })
    // },
    // getters: {
    //     get_socket: (state) => state.socket
    // }
}

export default socket